{
  "name": "average-weather-backend",
  "version": "1.0.0",
  "description": "average weather by location app backend",
  "main": "index.js",
  "scripts": {
    "start":"node server.js",
    "dev": "nodemon server.js",
    "build": "NODE_ENV=production nodemon server.js",
    "authServer": "nodemon authServer.js",
    "buildauthServer": "NODE_ENV=production nodemon authServer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eduardo Marcos",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  },
  "engines": {
    "node":"10.0.0"
  }
}
